public class Main
{
	public static void main(String[] args) {
	    String s = "BA";
	    int n = s.length();
	    int ans = 0;
	    int c = s.length()-1;
		for(int i=0;i<n;i++){
		    ans+=(Math.pow(26,i)*(s.charAt(c)-'A'+1));
		    c = c-1;
		}
		System.out.print(ans);
	}
}
